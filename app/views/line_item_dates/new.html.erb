<main class="container">
  <%= link_to sanitize('&larr; Back to categories'), category_path(@category) %>

  <div class="header">
    <h1>New date</h1>
  </div>

  <%= render 'form', category: @category, line_item_date: @line_item_date %>
</main>
