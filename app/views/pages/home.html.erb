<main class="container">
  <h1>Journal</h1>
  <p>A blazing fast quote editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to 'View quotes', categories_path, class: 'btn btn--dark' %>
  <% else %>
    <%= link_to 'Sign in', new_user_session_path, class: 'btn btn--dark' %>
  <% end %>
</main>
